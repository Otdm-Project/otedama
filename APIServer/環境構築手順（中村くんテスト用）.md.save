# 環境構築手順
AWSでalmalinux9(x86_64)を用いてサーバを起動（SSH設定等は各自）

AlmaLinux OS 9 (x86_64)-9.4.20240805-AutogenByAWSMP--1
sg-0194c92a16c591d80

以下のコマンドを実行
1.2.3. と選択肢が出てくる場面があるが、そのままEnter
```
sudo dnf upgrade -y 
curl --proto '=https' --tlsv1.2 -sSf https://sh.rustup.rs | sh
source $HOME/.cargo/env
sudo dnf install -y gcc openssl-devel pkgconfig
```
completeと出ればOK
以下を実行
```
cargo new apiserver
cd apiserver
```
以下のコマンドを実行し、その下の内容を追加
```
vi Cargo.toml
```

```
[dependencies]
tokio = { version = "1", features = ["full"] }
tokio-tungstenite = "0.17"
tungstenite = "0.17"
```
以下のコマンドを実行し、その下の内容を記述
```
rm main.rs
vi main.rs
```

```

```















## 必要パッケージのインストール
```
sudo dnf install -y gcc openssl-devel pkgconfig
```
DB接続に必要になったもの
```
sudo dnf install python3-pip -y
pip install cqlsh
```
### Rustプロジェクトの作成
```
cargo new apiserver
cd apiserver
```

### 依存関係追加
* Cargo.tomlを記述

### プログラム追加
```
cd src
```
* main.rsを記述
* websocket.rsを記述

## APIClient
### Rustプロジェクトの作成
```
cargo new apiclient
cd apiclient
```
### 依存関係追加
* Cargo.tomlを記述

### プログラム追加
```
cd src
```
* main.rsを記述


